<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-01-06T17:24:18+00:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Xuan-Toan Dang Profile</title><subtitle>Xuan-Toan Dang&apos;s academic project</subtitle><author><name>Xuan-Toan Dang</name><email>dangxuantoan@soongsil.ac.kr</email></author><entry><title type="html">NAS-RaspberryPi-1</title><link href="http://localhost:4000/posts/2025/20251206/NAS-RaspberryPi1/" rel="alternate" type="text/html" title="NAS-RaspberryPi-1" /><published>2026-01-05T00:00:00+00:00</published><updated>2026-01-05T00:00:00+00:00</updated><id>http://localhost:4000/posts/2025/20251206/NAS-RaspberryPi1-blog-2</id><content type="html" xml:base="http://localhost:4000/posts/2025/20251206/NAS-RaspberryPi1/"><![CDATA[<hr />
<p>My friend gave me a device called a Raspberry Pi 1. At first, I didn’t know what to do with it because it is quite outdated for most modern use cases. However, I realized that I could still utilize this device by turning it into a Network Attached Storage (NAS). That is why I decided to explore a suitable way to configure it as a local NAS for my home. In this post, I will share what I did and what I learned from this experience.</p>

<p>This post guide shows how to turn a Raspberry Pi into a lightweight Network Attached Storage (NAS) using Samba. It covers connecting external USB/HDD/SSD storage, configuring automatic mounting with UUIDs, setting correct permissions, and sharing files across Windows, macOS, and Linux systems. The tutorial is designed to be beginner-friendly, command-</p>

<h1 id="️-turn-raspberry-pi-1-into-a-nas-using-samba-step-by-step">🗄️ Turn Raspberry Pi 1 into a NAS using Samba (Step-by-Step)</h1>

<blockquote>
  <p><strong>Goal</strong></p>

  <ul>
    <li>Use Raspberry Pi as a <strong>home NAS</strong></li>
    <li>Attach <strong>external USB / HDD / SSD storage</strong></li>
    <li>Share files via <strong>Samba</strong> (Windows / macOS / Linux)</li>
    <li>Ensure disks <strong>auto-mount on reboot</strong></li>
  </ul>
</blockquote>

<hr />

<h2 id="0-switch-to-root-optional-but-convenient">0) Switch to root (optional but convenient)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo</span> <span class="nt">-i</span>
</code></pre></div></div>

<p>👉 Enters root shell so you don’t need to type <code class="language-plaintext highlighter-rouge">sudo</code> for every command.<br />
(You can skip this and keep using <code class="language-plaintext highlighter-rouge">sudo</code> if you prefer.)</p>

<hr />

<h2 id="1-update-the-system">1) Update the system</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt update <span class="o">&amp;&amp;</span> apt upgrade <span class="nt">-y</span>
</code></pre></div></div>

<p>👉 Updates package lists and upgrades installed packages.<br />
(Always do this before installing new software.)</p>

<hr />

<h2 id="2-install-samba-and-required-tools">2) Install Samba and required tools</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt <span class="nb">install </span>samba blkid <span class="nt">-y</span>
</code></pre></div></div>

<p>👉</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">samba</code> → file sharing service</li>
  <li><code class="language-plaintext highlighter-rouge">blkid</code> → used to get disk UUIDs (required for <code class="language-plaintext highlighter-rouge">/etc/fstab</code>)</li>
</ul>

<hr />

<h2 id="3-plug-in-usb--hdd--ssd-and-detect-it">3) Plug in USB / HDD / SSD and detect it</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lsblk <span class="nt">-o</span> NAME,FSTYPE,SIZE,MOUNTPOINT
</code></pre></div></div>

<p>👉 Lists all storage devices.<br />
Note the partition name (example: <code class="language-plaintext highlighter-rouge">/dev/sda1</code>).</p>

<hr />

<h2 id="4-view-partition-details-and-uuid">4) View partition details and UUID</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>blkid /dev/sda1
</code></pre></div></div>

<p>👉 Displays filesystem type and <strong>UUID</strong>.<br />
(UUID is safer than device names for auto-mount.)</p>

<hr />

<h2 id="5-optional-format-disk-to-ext4-️-erases-all-data">5) (Optional) Format disk to ext4 ⚠️ ERASES ALL DATA</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkfs.ext4 /dev/sda1
</code></pre></div></div>

<p>👉 Formats the partition as <strong>ext4</strong> (recommended for Linux).<br />
⚠️ <strong>All existing data will be deleted</strong>.</p>

<hr />

<h2 id="6-create-mount-points">6) Create mount points</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /mnt/usb
<span class="nb">mkdir</span> <span class="nt">-p</span> /mnt/hdd
</code></pre></div></div>

<p>👉 Creates directories where external storage will be mounted.</p>

<hr />

<h2 id="7-temporarily-mount-the-disk-test">7) Temporarily mount the disk (test)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mount /dev/sda1 /mnt/usb
</code></pre></div></div>

<p>👉 Tests whether the disk mounts correctly before enabling auto-mount.</p>

<h3 id="verify">Verify</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">df</span> <span class="nt">-h</span> | <span class="nb">grep</span> /mnt/usb
<span class="nb">ls</span> <span class="nt">-l</span> /mnt/usb
</code></pre></div></div>

<p>👉 Confirms the disk is mounted and accessible.</p>

<hr />

<h2 id="8-copy-uuid-for-etcfstab">8) Copy UUID for <code class="language-plaintext highlighter-rouge">/etc/fstab</code></h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>blkid /dev/sda1
</code></pre></div></div>

<p>👉 Copy the value inside <code class="language-plaintext highlighter-rouge">UUID="..."</code>.</p>

<hr />

<h2 id="9-configure-auto-mount-using-etcfstab">9) Configure auto-mount using <code class="language-plaintext highlighter-rouge">/etc/fstab</code></h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano /etc/fstab
</code></pre></div></div>

<h3 id="if-the-disk-is-fat32--vfat-cross-platform-usb">If the disk is <strong>FAT32 / vfat</strong> (cross-platform USB)</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UUID=ABCD-1234  /mnt/usb  vfat  defaults,uid=xt234,gid=xt234,umask=022,noatime  0  0
</code></pre></div></div>

<p>👉 <code class="language-plaintext highlighter-rouge">uid/gid</code> ensures user <code class="language-plaintext highlighter-rouge">xt234</code> can write to FAT32.</p>

<h3 id="if-the-disk-is-ext4">If the disk is <strong>ext4</strong></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UUID=EEEE-FFFF  /mnt/hdd  ext4  defaults,noatime  0  2
</code></pre></div></div>

<p>👉 ext4 keeps Linux permissions internally.</p>

<p>Save and exit: <strong>Ctrl+O → Enter → Ctrl+X</strong></p>

<hr />

<h2 id="10-apply-and-test-fstab">10) Apply and test fstab</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mount <span class="nt">-a</span>
</code></pre></div></div>

<p>👉 Mounts all filesystems in <code class="language-plaintext highlighter-rouge">/etc/fstab</code>.<br />
(No output = no errors.)</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">df</span> <span class="nt">-h</span> | egrep <span class="s1">'/mnt/usb|/mnt/hdd'</span>
</code></pre></div></div>

<p>👉 Confirms auto-mount works.</p>

<hr />

<h2 id="11-fix-permissions-important-for-samba">11) Fix permissions (important for Samba)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chown</span> <span class="nt">-R</span> xt234:xt234 /mnt/usb
<span class="nb">chown</span> <span class="nt">-R</span> xt234:xt234 /mnt/hdd
<span class="nb">chmod </span>755 /mnt/usb
<span class="nb">chmod </span>755 /mnt/hdd
</code></pre></div></div>

<p>👉 Ensures Samba user owns the storage and can access it safely.</p>

<hr />

<h2 id="12-create-shared-directories">12) Create shared directories</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /mnt/hdd/shared
<span class="nb">chown</span> <span class="nt">-R</span> xt234:xt234 /mnt/hdd/shared
</code></pre></div></div>

<p>👉 Organizes folders to be shared via Samba.</p>

<hr />

<h2 id="13-create-samba-user-maps-to-linux-user">13) Create Samba user (maps to Linux user)</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>smbpasswd <span class="nt">-a</span> xt234
</code></pre></div></div>

<p>👉 Sets Samba password for user <code class="language-plaintext highlighter-rouge">xt234</code>.<br />
(User must already exist: <code class="language-plaintext highlighter-rouge">adduser xt234</code> if needed.)</p>

<hr />

<h2 id="14-configure-samba-shares">14) Configure Samba shares</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /etc/samba/smb.conf /etc/samba/smb.conf.bak
nano /etc/samba/smb.conf
</code></pre></div></div>

<h3 id="add-at-the-bottom">Add at the bottom:</h3>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[USB]</span>
   <span class="py">path</span> <span class="p">=</span> <span class="s">/mnt/usb</span>
   <span class="py">browseable</span> <span class="p">=</span> <span class="s">yes</span>
   <span class="err">read</span> <span class="py">only</span> <span class="p">=</span> <span class="s">no</span>
   <span class="err">valid</span> <span class="py">users</span> <span class="p">=</span> <span class="s">xt234</span>
   <span class="err">create</span> <span class="py">mask</span> <span class="p">=</span> <span class="s">0644</span>
   <span class="err">directory</span> <span class="py">mask</span> <span class="p">=</span> <span class="s">0755</span>

<span class="nn">[HDD]</span>
   <span class="py">path</span> <span class="p">=</span> <span class="s">/mnt/hdd/shared</span>
   <span class="py">browseable</span> <span class="p">=</span> <span class="s">yes</span>
   <span class="err">read</span> <span class="py">only</span> <span class="p">=</span> <span class="s">no</span>
   <span class="err">valid</span> <span class="py">users</span> <span class="p">=</span> <span class="s">xt234</span>
   <span class="err">create</span> <span class="py">mask</span> <span class="p">=</span> <span class="s">0644</span>
   <span class="err">directory</span> <span class="py">mask</span> <span class="p">=</span> <span class="s">0755</span>
</code></pre></div></div>

<p>👉 Defines two Samba shares: USB and HDD.</p>

<hr />

<h2 id="15-restart-samba-and-enable-auto-start">15) Restart Samba and enable auto-start</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl restart smbd nmbd
systemctl <span class="nb">enable </span>smbd nmbd
systemctl status smbd <span class="nt">--no-pager</span>
</code></pre></div></div>

<p>👉 Applies config, enables Samba at boot, and checks service status.</p>

<hr />

<h2 id="-result">✅ Result</h2>

<ul>
  <li>Raspberry Pi works as a <strong>NAS</strong></li>
  <li>External storage auto-mounts on boot</li>
  <li>Files accessible from:
    <ul>
      <li><strong>Windows</strong> → <code class="language-plaintext highlighter-rouge">\\raspi-ip\HDD</code></li>
      <li><strong>macOS/Linux</strong> → <code class="language-plaintext highlighter-rouge">smb://raspi-ip/HDD</code></li>
    </ul>
  </li>
</ul>

<hr />

<h2 id="-optional-next-steps">🔧 Optional next steps</h2>

<ul>
  <li>Static IP</li>
  <li>Webmin management</li>
  <li>Samba recycle bin</li>
  <li>User quotas</li>
  <li>RAID (mdadm)</li>
</ul>

<hr />]]></content><author><name>Xuan-Toan Dang</name><email>dangxuantoan@soongsil.ac.kr</email></author><category term="cool posts" /><summary type="html"><![CDATA[My friend gave me a device called a Raspberry Pi 1. At first, I didn’t know what to do with it because it is quite outdated for most modern use cases. However, I realized that I could still utilize this device by turning it into a Network Attached Storage (NAS). That is why I decided to explore a suitable way to configure it as a local NAS for my home. In this post, I will share what I did and what I learned from this experience.]]></summary></entry><entry><title type="html">SAC Algorithm</title><link href="http://localhost:4000/posts/2025/20251206/SAC-algorithm/" rel="alternate" type="text/html" title="SAC Algorithm" /><published>2025-12-06T00:00:00+00:00</published><updated>2025-12-06T00:00:00+00:00</updated><id>http://localhost:4000/posts/2025/20251206/SAC-Algorithm-blog-1</id><content type="html" xml:base="http://localhost:4000/posts/2025/20251206/SAC-algorithm/"><![CDATA[<p>The <strong>Soft Actor–Critic (SAC)</strong> algorithm is one of the most influential advancements in modern reinforcement learning (RL), especially in continuous control tasks. Although its origins lie in machine learning, the algorithm is deeply rooted in <em>mathematics</em>—from stochastic calculus to optimization theory and information entropy. This blog will walk you through the mathematical foundations and inner mechanics of SAC in a structured, intuitive way.</p>

<hr />

<h2 id="-what-is-sac">🔍 What Is SAC?</h2>

<p>SAC is an <strong>off-policy, model-free, actor–critic RL algorithm</strong> that optimizes a stochastic policy in an environment with continuous actions. Its distinguishing feature is <strong>entropy regularization</strong>, which encourages exploration and yields more stable learning.</p>

<p>SAC blends three major elements:</p>

<ol>
  <li><strong>Maximum Entropy Reinforcement Learning</strong></li>
  <li><strong>Actor–Critic Framework</strong></li>
  <li><strong>Soft Q-Learning</strong></li>
</ol>

<hr />

<h1 id="soft-actorcritic-sac-algorithm--detailed-mathematical-blog">Soft Actor–Critic (SAC) Algorithm — Detailed Mathematical Blog</h1>

<h2 id="-mathematical-foundations">📐 Mathematical Foundations</h2>

<h3 id="1-maximum-entropy-objective">1. Maximum Entropy Objective</h3>

<p>Standard RL maximizes the expected sum of rewards:</p>

\[J(\pi) = \sum_{t=0}^{\infty} \mathbb{E}_{\pi}\left[ r(s_t, a_t) \right]\]

<p>SAC uses the <strong>maximum entropy objective</strong>, enhancing exploration and improving robustness:</p>

\[J_{\text{max-ent}}(\pi) = 
\sum_{t=0}^{\infty} 
\mathbb{E}_{\pi} 
\left[
    r(s_t, a_t) 
    + 
    \alpha \mathcal{H}(\pi(\cdot \mid s_t))
\right]\]

<p>where:</p>

<ul>
  <li>
\[\mathcal{H}(\pi(\cdot \mid s_t)) = -\mathbb{E}[ \log \pi(a_t \mid s_t) ]\]
  </li>
  <li>\(\alpha\) is the temperature parameter controlling the entropy–reward trade-off</li>
</ul>

<p>This objective encourages <strong>exploratory yet reward-driven</strong> policies.</p>

<hr />

<h2 id="-the-components-of-sac">🧠 The Components of SAC</h2>

<p>SAC maintains <strong>three neural networks</strong>:</p>

<ol>
  <li>
    <p><strong>Two Q-functions</strong><br />
\(Q_{\theta_1}(s,a),\; Q_{\theta_2}(s,a)\)</p>
  </li>
  <li>
    <p><strong>A stochastic actor (policy)</strong><br />
\(\pi_\phi(a \mid s)\)</p>
  </li>
</ol>

<p>Using two Q-networks reduces overestimation bias.</p>

<hr />

<h2 id="-1-the-soft-q-function-loss">🔢 1. The Soft Q-Function Loss</h2>

<p>The target for Q-learning includes the entropy term:</p>

\[y = 
r(s_t, a_t) 
+ \gamma 
\mathbb{E}_{a_{t+1} \sim \pi}
\left[
    \min_{i=1,2} Q_{\theta_i}(s_{t+1}, a_{t+1})
    - 
    \alpha \log \pi(a_{t+1} \mid s_{t+1})
\right]\]

<p>The loss for each Q-function is:</p>

\[J_Q(\theta_i) 
= 
\mathbb{E}
\left[
    (Q_{\theta_i}(s_t,a_t) - y)^2
\right]\]

<hr />

<h2 id="-2-the-policy-actor-loss">🔢 2. The Policy (Actor) Loss</h2>

<p>The actor improves by minimizing:</p>

\[J_\pi(\phi)
=
\mathbb{E}_{s_t \sim \mathcal{D}}
\Bigg[
    \mathbb{E}_{a_t \sim \pi_\phi}
    \left[
        \alpha \log \pi_\phi(a_t \mid s_t)
        -
        \min_{i=1,2}
        Q_{\theta_i}(s_t, a_t)
    \right]
    \Bigg]\]

<p>This trades off:</p>

<ul>
  <li>better Q-values</li>
  <li>higher entropy (diverse actions)</li>
</ul>

<hr />

<h2 id="-entropy-temperature-adjustment-optional">🔥 Entropy Temperature Adjustment (Optional)</h2>

<p>SAC can automatically adjust \(\alpha\) by minimizing:</p>

\[J(\alpha)
=
\mathbb{E}_{a_t \sim \pi}
\left[
    -\alpha
    \left(
        \log \pi(a_t \mid s_t)
        + 
        \mathcal{H}_{\text{target}}
    \right)
\right]\]

<p>This ensures the entropy stays close to a chosen target.</p>

<hr />

<h2 id="️-algorithm-outline">🏗️ Algorithm Outline</h2>

<ol>
  <li>Initialize Q-networks, policy network, and target Q-networks</li>
  <li>
    <p>For each interaction step:</p>

    <ul>
      <li>Sample action from the stochastic policy</li>
      <li>Store transition in replay buffer</li>
    </ul>
  </li>
  <li>
    <p>For each gradient step:</p>

    <ul>
      <li>Update Q-networks using soft Bellman backup</li>
      <li>Update policy via entropy-regularized objective</li>
      <li>Update temperature ( \alpha ) if enabled</li>
      <li>Soft-update target networks</li>
    </ul>
  </li>
</ol>

<hr />

<h2 id="-why-sac-works-so-well">📊 Why SAC Works So Well</h2>

<h3 id="-stability">✔ Stability</h3>

<p>Entropy regularization smooths updates and prevents premature convergence.</p>

<h3 id="-robust-exploration">✔ Robust Exploration</h3>

<p>Stochasticity gives better performance in sparse or noisy reward tasks.</p>

<h3 id="-sample-efficiency">✔ Sample Efficiency</h3>

<p>As an off-policy method, SAC reuses past experience effectively.</p>

<h3 id="-continuous-action-suitability">✔ Continuous Action Suitability</h3>

<p>Perfect for robotics, control engineering, and physics-based environments.</p>

<hr />

<h2 id="-sac-vs-other-rl-algorithms">🧮 SAC vs. Other RL Algorithms</h2>

<table>
  <thead>
    <tr>
      <th>Algorithm</th>
      <th>On/Off-Policy</th>
      <th>Deterministic?</th>
      <th>Key Strength</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DDPG</td>
      <td>Off-policy</td>
      <td>Deterministic</td>
      <td>Fast but less stable</td>
    </tr>
    <tr>
      <td>TD3</td>
      <td>Off-policy</td>
      <td>Deterministic</td>
      <td>Reduced overestimation</td>
    </tr>
    <tr>
      <td>PPO</td>
      <td>On-policy</td>
      <td>Stochastic</td>
      <td>Stable but less sample-efficient</td>
    </tr>
    <tr>
      <td><strong>SAC</strong></td>
      <td><strong>Off-policy</strong></td>
      <td><strong>Stochastic</strong></td>
      <td><strong>Exploration + stability + efficiency</strong></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-summary">🧾 Summary</h2>

<p>The SAC algorithm is mathematically elegant and computationally effective. Its use of entropy regularization represents a shift toward more robust and exploratory learning strategies. With strong theoretical foundations and empirical results, SAC remains a state-of-the-art algorithm for continuous-control problems.</p>

<hr />]]></content><author><name>Xuan-Toan Dang</name><email>dangxuantoan@soongsil.ac.kr</email></author><category term="cool posts" /><category term="category1" /><category term="category2" /><summary type="html"><![CDATA[The Soft Actor–Critic (SAC) algorithm is one of the most influential advancements in modern reinforcement learning (RL), especially in continuous control tasks. Although its origins lie in machine learning, the algorithm is deeply rooted in mathematics—from stochastic calculus to optimization theory and information entropy. This blog will walk you through the mathematical foundations and inner mechanics of SAC in a structured, intuitive way.]]></summary></entry></feed>